<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quiztest1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuizTest1">
        <activity android:name=".FAQActivity"></activity>
        <activity android:name=".AboutActivity" />
        <activity android:name=".LevelChoiceActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LeaderboardActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".FriendsActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".N5EnglishActivity" />
        <activity android:name=".N5KanaActivity" />
        <activity android:name=".EndlessActivity" />
    </application>

</manifest>